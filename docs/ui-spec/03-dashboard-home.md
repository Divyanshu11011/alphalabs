# AlphaLab UI/UX Specification
## Part 3: Dashboard Home Screen

---

## 📍 SCREEN OVERVIEW

**Route:** `/dashboard`
**Purpose:** Central hub showing overview stats, recent activity, quick actions, and system status.

---

## 🖼️ FULL LAYOUT DIAGRAM

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│                              DASHBOARD HOME                                          │
├─────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐    │
│  │  HEADER                                                                      │    │
│  │  ┌────────────────────────────────────┐  ┌───────────────────────────────┐  │    │
│  │  │ Welcome back, Alex                 │  │ Quick Actions                 │  │    │
│  │  │ Ready to train some fighters?      │  │ [+ New Agent] [⚡ Quick Test] │  │    │
│  │  └────────────────────────────────────┘  └───────────────────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐    │
│  │  STATS ROW (4 Cards)                                                         │    │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐            │    │
│  │  │ Total Agents│ │ Tests Run   │ │ Best PnL    │ │ Win Rate    │            │    │
│  │  │             │ │             │ │             │ │             │            │    │
│  │  │     3       │ │     27      │ │   +47.2%    │ │    62%      │            │    │
│  │  │  ↑1 this wk │ │  ↑5 today   │ │  Agent: α-1 │ │  ↑3% avg    │            │    │
│  │  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘            │    │
│  └─────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                      │
│  ┌────────────────────────────────────────────┐ ┌──────────────────────────────┐    │
│  │  RECENT ACTIVITY                           │ │  LIVE SESSIONS               │    │
│  │  (Table/List)                              │ │  (Status Cards)              │    │
│  │                                            │ │                              │    │
│  │  ┌──────────────────────────────────────┐  │ │  ┌────────────────────────┐  │    │
│  │  │ 🟢 α-1 Backtest Complete    +23.4%  │  │ │  │  🔴 No active sessions │  │    │
│  │  │    2 hours ago              View →  │  │ │  │                        │  │    │
│  │  └──────────────────────────────────────┘  │ │  │  Start a Forward Test  │  │    │
│  │  ┌──────────────────────────────────────┐  │ │  │  to see live trading   │  │    │
│  │  │ 🔴 β-2 Backtest Failed      -12.1%  │  │ │  │                        │  │    │
│  │  │    5 hours ago              View →  │  │ │  │  [Start Test →]        │  │    │
│  │  └──────────────────────────────────────┘  │ │  └────────────────────────┘  │    │
│  │  ┌──────────────────────────────────────┐  │ │                              │    │
│  │  │ 🟡 γ-3 Agent Created               │  │ │  ─── OR if active: ───       │    │
│  │  │    Yesterday                Edit → │  │ │                              │    │
│  │  └──────────────────────────────────────┘  │ │  ┌────────────────────────┐  │    │
│  │                                            │ │  │  🟢 α-1 LIVE           │  │    │
│  │  [View All Activity →]                     │ │  │  Running 4h 23m        │  │    │
│  │                                            │ │  │  PnL: +2.3%            │  │    │
│  └────────────────────────────────────────────┘ │  │  Trades: 3             │  │    │
│                                                  │  │  [View →] [Pause]      │  │    │
│                                                  │  └────────────────────────┘  │    │
│                                                  └──────────────────────────────┘    │
│                                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────────────┐    │
│  │  QUICK START GUIDE (Collapsible, shows for new users)                        │    │
│  │  ──────────────────────────────────────────────────────────────────────────  │    │
│  │  ○ Create your first agent  ──────●────────────────────────────────────────  │    │
│  │  ○ Run a backtest           ──────────────────────────────────────────────── │    │
│  │  ○ Generate your first certificate  ─────────────────────────────────────── │    │
│  │                                                                 [Dismiss ✕]  │    │
│  └─────────────────────────────────────────────────────────────────────────────┘    │
│                                                                                      │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🧱 COMPONENT BREAKDOWN

### 1. PAGE HEADER

```
Component: DashboardHeader
├── Left Section
│   ├── Greeting: "Welcome back, {firstName}"
│   │   └── Dynamic based on time: "Good morning/afternoon/evening"
│   └── Subtitle: "Ready to train some fighters?"
│       └── Variations: Random motivational trading quotes
│
├── Right Section
│   └── Quick Actions Group
│       ├── Primary Button: "+ New Agent"
│       │   └── Action: Navigate to /agents/new
│       └── Secondary Button: "⚡ Quick Test"
│           └── Action: Open Quick Test Modal (pre-select agent, start backtest)
│
└── Layout: flex, justify-between, items-center
```

**shadcn Components:**
- `Button` (variant: default for primary, outline for secondary)

---

### 2. STATS CARDS ROW

```
Component: StatsCardRow
├── Layout: grid grid-cols-4 gap-4 (responsive: 2 cols on tablet, 1 on mobile)
│
└── Cards:
    ├── StatCard: Total Agents
    │   ├── Icon: Bot (top-left, text-muted)
    │   ├── Value: {count} (32px, bold, text-primary)
    │   ├── Label: "Total Agents" (12px, text-muted)
    │   └── Trend: "↑1 this week" (text-green or text-red)
    │
    ├── StatCard: Tests Run
    │   ├── Icon: FlaskConical
    │   ├── Value: {count}
    │   ├── Label: "Tests Run"
    │   └── Trend: "↑5 today"
    │
    ├── StatCard: Best PnL
    │   ├── Icon: TrendingUp
    │   ├── Value: "+{pnl}%" (text-green if positive)
    │   ├── Label: "Best PnL"
    │   └── SubLabel: "Agent: {agentName}"
    │
    └── StatCard: Win Rate
        ├── Icon: Target
        ├── Value: "{rate}%"
        ├── Label: "Avg Win Rate"
        └── Trend: "↑3% vs last month"
```

**StatCard Component Structure:**
```
┌─────────────────────────────────┐
│ [Icon]              ┌─────────┐│
│                     │ Sparkline│ (optional mini chart)
│   47.2%             └─────────┘│
│   Best PnL                     │
│   Agent: α-1        ↑12.3%     │
└─────────────────────────────────┘
```

**shadcn Components:**
- `Card` + `CardContent`
- Custom `StatCard` wrapper

---

### 3. RECENT ACTIVITY SECTION

```
Component: RecentActivity
├── Header
│   ├── Title: "Recent Activity"
│   └── Link: "View All →" → /results
│
├── Activity List
│   └── ActivityItem (repeated)
│       ├── Status Indicator
│       │   ├── 🟢 Green: Success/Profitable
│       │   ├── 🔴 Red: Failed/Loss
│       │   └── 🟡 Yellow: Pending/Created
│       │
│       ├── Content
│       │   ├── Agent Name: Bold, 14px
│       │   ├── Action: "Backtest Complete" / "Agent Created" / etc.
│       │   └── Timestamp: "2 hours ago" (text-muted)
│       │
│       ├── Result Badge (if applicable)
│       │   ├── +23.4% (bg-green/10, text-green)
│       │   └── -12.1% (bg-red/10, text-red)
│       │
│       └── Action Link
│           └── "View →" or "Edit →"
│
├── Empty State
│   ├── Icon: Inbox (large, muted)
│   ├── Title: "No activity yet"
│   ├── Description: "Create an agent and run your first test"
│   └── CTA: [Create Agent]
│
└── Layout: Card container with list inside
```

**Activity Item Layout:**
```
┌──────────────────────────────────────────────────────────────────┐
│ 🟢  α-1 Backtest Complete                      +23.4%   View →  │
│     2 hours ago • Monk Mode • BTC/USDT                          │
└──────────────────────────────────────────────────────────────────┘
```

**shadcn Components:**
- `Card` + `CardHeader` + `CardContent`
- `Badge` for status and PnL
- `Button` (variant: link)

---

### 4. LIVE SESSIONS PANEL

```
Component: LiveSessionsPanel
├── Header
│   └── Title: "Live Sessions"
│
├── State A: No Active Sessions
│   ├── Icon: Radio (large, muted, with pulse animation)
│   ├── Title: "No active sessions"
│   ├── Description: "Start a Forward Test to see live trading"
│   └── CTA Button: [Start Test →]
│
├── State B: Active Session(s)
│   └── LiveSessionCard (repeated for each active)
│       ├── Header Row
│       │   ├── Status Dot: 🟢 (pulsing animation)
│       │   ├── Agent Name: "α-1"
│       │   └── Badge: "LIVE"
│       │
│       ├── Stats Grid (2x2)
│       │   ├── Duration: "4h 23m"
│       │   ├── PnL: "+2.3%"
│       │   ├── Trades: "3"
│       │   └── Win Rate: "66%"
│       │
│       ├── Mini Chart (optional)
│       │   └── Equity curve sparkline
│       │
│       └── Actions Row
│           ├── [View →] Primary
│           └── [Pause] Ghost
│
└── Layout: Card container, stacked sessions
```

**Live Session Card Layout:**
```
┌────────────────────────────────────────┐
│ 🟢 α-1                          [LIVE] │
├────────────────────────────────────────┤
│ Duration      PnL                      │
│ 4h 23m        +2.3%                    │
│                                        │
│ Trades        Win Rate                 │
│ 3             66%                      │
├────────────────────────────────────────┤
│ [View Session →]         [⏸ Pause]    │
└────────────────────────────────────────┘
```

**shadcn Components:**
- `Card` + `CardHeader` + `CardContent` + `CardFooter`
- `Badge` (variant: default with green styling)
- `Button`

---

### 5. QUICK START GUIDE (Onboarding)

```
Component: QuickStartGuide
├── Visibility: Only shown if user.hasCompletedOnboarding === false
│
├── Header
│   ├── Title: "Quick Start Guide"
│   └── Dismiss Button: [✕]
│
├── Progress Steps
│   ├── Step 1: "Create your first agent"
│   │   ├── Status: complete | current | upcoming
│   │   ├── Icon: Check (complete) | Circle (current) | Circle (upcoming)
│   │   └── Progress Bar: Connecting line between steps
│   │
│   ├── Step 2: "Run a backtest"
│   │
│   └── Step 3: "Generate your first certificate"
│
├── Current Step CTA
│   └── Button relevant to current incomplete step
│
└── Dismiss Action
    └── Sets user.hasCompletedOnboarding = true (or dismissed)
```

**shadcn Components:**
- `Card` + `Collapsible`
- `Progress`
- Custom step indicators

---

## 📱 RESPONSIVE BEHAVIOR

```
┌─────────────────────────────────────────────────────────────────┐
│  DESKTOP (≥1280px)                                               │
├─────────────────────────────────────────────────────────────────┤
│  - Stats: 4 columns                                              │
│  - Activity + Live Sessions: Side by side (8:4 ratio)           │
│  - Full content visible                                          │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  TABLET (768px - 1279px)                                         │
├─────────────────────────────────────────────────────────────────┤
│  - Stats: 2 columns (2x2 grid)                                   │
│  - Activity + Live Sessions: Stacked vertically                  │
│  - Live Sessions moves below Activity                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  MOBILE (<768px)                                                 │
├─────────────────────────────────────────────────────────────────┤
│  - Header: Stacked (greeting above, actions below)               │
│  - Stats: 1 column (scrollable horizontal as cards?)            │
│  - Activity: Full width, fewer items shown                       │
│  - Live Sessions: Full width, below activity                     │
│  - Quick Start: Full width, collapsible                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 DATA FETCHING

```typescript
// Dashboard data requirements
interface DashboardData {
  stats: {
    totalAgents: number;
    testsRun: number;
    bestPnL: { value: number; agentName: string };
    avgWinRate: number;
    trends: {
      agentsThisWeek: number;
      testsToday: number;
      winRateChange: number;
    };
  };
  recentActivity: ActivityItem[];
  liveSessions: LiveSession[];
  onboarding: {
    isComplete: boolean;
    currentStep: 1 | 2 | 3;
  };
}

// Fetch on page load
// Polling for live sessions: every 30 seconds
// Activity: Real-time via WebSocket or polling
```

---

## 🎨 SPECIAL STYLING NOTES

1. **Stats Cards Hover Effect:**
   - Subtle lift (transform: translateY(-2px))
   - Border glow matching stat color

2. **Live Session Pulse:**
   - Green dot has CSS pulse animation
   - `@keyframes pulse { 0%, 100% { opacity: 1 } 50% { opacity: 0.5 } }`

3. **Activity Item Hover:**
   - Background shifts to bg-elevated
   - Action link becomes visible

4. **Positive/Negative Colors:**
   - Always use semantic colors (green for profit, red for loss)
   - Include icon indicators for accessibility

---

## 🔗 COMPONENT FILE STRUCTURE

```
/components/dashboard/
├── dashboard-header.tsx
├── stats-card.tsx
├── stats-card-row.tsx
├── recent-activity.tsx
├── activity-item.tsx
├── live-sessions-panel.tsx
├── live-session-card.tsx
└── quick-start-guide.tsx
```

---

**← Back to Part 2: Sidebar | Continue to Part 4: My Agents List →**

